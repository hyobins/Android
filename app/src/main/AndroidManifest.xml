<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.swe_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="Lecture3.AddressActivity"></activity>
        <activity android:name="Lecture3.ListViewActivity_2" />
        <activity android:name=".LifeCycleActivity" />
        <activity android:name="Lecture3.ListViewActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name="Week1.CalcActivity" />
        <activity android:name="Week1.CountActivity" />
        <activity android:name="Week1.ImgChangeActivity2" />
        <activity android:name="Week1.ImgChangeActivity" />
        <activity android:name="Week1.LinearActivity_1" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>